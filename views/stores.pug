extends layout

include mixins/_storeCard

block content
  .inner
    h2= title
    // pre= h.dump(stores) // dump is set in './helpers.js'
    .stores
      each store in stores
        +storeCard(store)
